{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{category.title}}</h1>

    {% for quiz in category.quizzes %}
    <li><a href="{{ path('app_quiz', {id: quiz.id})}}">{{quiz.title}}</li></a>
    {% endfor %}
{% endblock %}
